{
  "name": "identity-security-dashboard",
  "version": "1.2.0",
  "description": "Cloudflare Worker dashboard to audit Microsoft Entra ID and OneLogin MFA misconfigurations.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
